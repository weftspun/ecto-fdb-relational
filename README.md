# EctoFdbRelational

FRL is Apple's SQL layer on top of FoundationDB's Record Layer (a mature
Java library for storing structured/Protobuf-backed records in FDB). It has
its own DDL/SQL dialect that is close to but **not** standard SQL:
